/**
 * Get the default branch from a remote repository
 */
export declare const getDefaultBranchFromRemote: (repository: string) => Promise<string | undefined>;
export declare const syncForkRepository: (key: string, { branch, upstream, fork, }: {
    branch?: string;
    upstream: string;
    fork: string;
}) => Promise<void>;
export declare const cloneRepository: (key: string, { repositoryUrl, extraName, shallow, branch, tmpDir, }: {
    repositoryUrl: string;
    extraName?: string;
    shallow?: boolean;
    branch?: string;
    tmpDir: string;
}) => Promise<void>;
