import MagicString from "magic-string";
import { Context } from "./Context.js";
export type FileContextData = {
    file: string;
};
export declare class FileContext extends Context<FileContextData> {
    private _contents;
    private _magicString;
    private _contentsChanged;
    private _oldContents;
    importsUpdates: {
        type: "add" | "remove";
        import: string;
    }[];
    get file(): string;
    contents(): Promise<string>;
    setContents(contents: string): void;
    magicString(): Promise<MagicString>;
    update({ start, end, replacement, }: {
        start: number;
        end: number;
        replacement: string;
    }): Promise<void>;
    save({ format }?: {
        format?: boolean;
    }): Promise<void>;
    get extension(): string | undefined;
}
