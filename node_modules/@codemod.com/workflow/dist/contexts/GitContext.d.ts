import { Context } from "./Context.js";
export type GitContextData = {
    repository: string;
    id: string;
};
export declare class GitContext extends Context<GitContextData> {
    get simpleGit(): import("simple-git").SimpleGit;
    checkout({ branch, force }: {
        branch: string;
        force?: boolean;
    }): Promise<void>;
}
