import type { PLazy } from "./PLazy.js";
/**
 * Run a command in current working directory
 * @param command The command to run
 * @param args Arguments to pass to the command
 *
 * @example
 * Simple run
 * ```ts
 * await exec("ls");
 * ```
 *
 * @example
 * Run with arguments
 * ```ts
 * await exec("ls", ["-al"]);
 * ```
 *
 * @example
 * Chaining commands
 * ```ts
 * await exec("ls")
 *   .exec("pwd")
 *   .exec("ls", ["-al"]);
 * ```
 */
export declare function execLogic(command: string, args?: string[]): PLazy<ExecHelpers> & ExecHelpers;
export declare const exec: typeof execLogic;
declare const execHelpers: {
    exec: typeof execLogic;
    codemod: typeof import("./codemod.js").codemodLogic;
};
type ExecHelpers = typeof execHelpers;
export {};
